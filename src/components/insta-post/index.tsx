import { View, Text } from "react-native";
import React from "react";
import { Body, Footer, Header } from "./elements";

const Post = () => {
  return (
    <View>
      <Header />
      <Body />
      <Footer />
    </View>
  );
};

export default Post;
